### 📊 13. 壓測環境下的數據一致性檢驗與修正紀錄

在進行 2000 QPS 的極限高並發壓測時，系統曾觀測到一項經典的分散式異步時空錯位現象：
* **現象**：前線壓測報告顯示 `2000/2000` 全數進單成功，但後台 Worker 日誌卻密集噴出 `啟動快取還原 (BUSINESS_LIMIT_EXCEEDED)`。
* **根因分析**：前線 API（ExecuteOrder）雖然引進了 `FOR UPDATE` 悲觀鎖，但在審查通過後，變更僅留存在 Go 協程記憶體與任務隊列快照中，並未即時對實體庫存進行預扣 Commit。導致後續排隊的並發請求撈到的依舊是舊的庫存水位，造成髒流量全數漏進消息佇列，迫使後台 Worker 必須頻繁觸發自我療癒的逆向補償。
* **架構優化**：後續將前線調整為 [實打實的資料庫行鎖預扣 / Redis 記憶體原子預扣]，讓前線安檢門具備即時攔截髒流量的能力，徹底達成全鏈路的數據最終一致性。





Docker 是一種容器化技術，它允許您將應用程式及其所有的依賴項（如資料庫驅動、特定的作業系統函式庫、配置檔案）打包在一個稱為「映像檔 (Image)」的標準單元中。

Docker 如何運作：

映像檔 (Image)：這是一個唯讀的模板，包含了執行應用程式所需的所有環境。

容器 (Container)：這是映像檔的執行實例。當您啟動映像檔時，它就變成一個容器。就像是您安裝並啟動後的遊戲。

Docker Compose：這是一個用於定義和執行多容器應用程式的工具。在 project-nm 中，我們需要同時執行 Go、PostgreSQL 和 Redis，Compose 就像是一個指揮官，確保大家一起正確啟動。

如何導入與運行

步驟 1：啟動環境
在您的專案根目錄執行以下指令：

docker-compose up -d

步驟 2：查看運行狀態
您可以確認容器是否都成功運作：

docker ps


步驟 3：進入容器內部 (如果需要除錯)
如果您想進入運行中的 app 容器查看檔案：

docker exec -it nm-app sh

為什麼要這樣設計？
隔離性：容器內部的 PostgreSQL 不會知道您本機也有一個 5432 的資料庫，因為它們在不同的虛擬網路中。

可攜性：無論您在公司還是家裡，只要有 Docker，執行 docker-compose up 就能得到完全一樣的環境。

效能：Docker 不是虛擬機（VM），它共享宿主機核心，因此對於 QPS 2000 的情境，它能提供接近原生系統的效能。



📌 Nginx
角色：反向代理 / 負載平衡器

用途：

把大量使用者請求分流到多個 Golang Order Service 節點。

提供 SSL/TLS 加密，確保交易安全。

可以做 API Gateway，統一入口管理 REST API 與 WebSocket。

優勢：

高效能、低資源消耗。

適合高併發金融交易場景。

📌 Message Queue (MQ)
選擇：Kafka、RabbitMQ、Redis Stream 都可。

用途：

當使用者下單後，訂單訊息先進入 MQ。

Order Service 從 MQ 消費訂單，確保即使流量暴增也不會丟單。

Notification Service 也可以從 MQ 消費交易結果，推播給前端。

優勢：

解耦系統，避免單一服務壓力過大。

提供可靠的訊息傳遞，支援重試與持久化。


📌 GraphQL
角色：查詢接口層

用途：

前端即時看板可以用 GraphQL 查詢「帳戶餘額 + 訂單紀錄 + 推播狀態」。

相比 REST API，GraphQL 可以一次拿到需要的資料，減少多次請求。

優勢：

前端開發更靈活，能精準取得需要的欄位。

適合交易查詢、報表查詢等複雜資料需求。


Nginx：作為入口閘道器，負責 SSL 終止與負載平衡。

GraphQL：用於前端 API 查詢，減少資料過度擷取 (Over-fetching)，提升行動端效能。

gRPC：用於服務與服務之間 (Internal RPC) 的高效能二進位通訊，發揮 Golang 強大的併發優勢。

Message Queue (RabbitMQ)：實現非同步解耦。當請求量過大時，將非即時任務（如發送通知、大數據計算）丟入隊列，達到「削峰填谷」。



進入 Redis 容器：docker exec -it nm-redis redis-cli

輸入：KEYS token:*


在 Redis 中，過期的 Token（也就是設定了 TTL 的 Key）並不是在時間到的那一秒「立刻」全部從記憶體中消失。

因為如果 Redis 為了在時間到的一瞬間刪除 Key，而架設一個碼錶（Timer）去時時刻刻監控幾萬個 Token，CPU 就會被這個監控機制給榨乾，這在高併發下是無法接受的。

為此，Redis 採用了兩種非常聰明的非同步刪除策略來互相配合：

1. 惰性刪除 (Lazy Expiration)
這是最節省 CPU 資源的方式。當一個 Token 的時間過期了，Redis 絕對不會主動去動它，而是靜靜地讓它躺在記憶體裡。

直到下一次有任何請求（例如你的 Middleware）試圖去讀取（GET）這個 Token 時，Redis 會在讀取的瞬間攔截並檢查：「咦？這個 Token 的過期時間已經到了。」
此時，Redis 會做兩件事：

立刻把這個 Token 從記憶體刪除。

回傳 (nil)（也就是 Go 語言收到的 redis.Nil 錯誤）給你的系統。

白話來說：只要沒人來查，過期的 Token 就會先裝死躺著；有人來查，立刻現形並當場處決。

2. 定期刪除 (Periodic Random Sampling)
如果有些 Token 過期了，但前端 App 已經被使用者砍掉，這輩子再也不會有人拿這個 Token 來打 API（也就是永遠不會觸發惰性刪除），那記憶體不就慢慢被這些「殭屍 Token」塞爆了嗎？

為了防止這種情況，Redis 內部有一個背景任務（Background Task）：

頻率：預設每秒執行 10 次（100ms 一次）。

行為：它會隨機抽選 20 個設定了過期時間的 Key。protoc --proto_path=pkg/grpc --go_out=pkg/grpc --go-grpc_out=pkg/grpc pkg/grpc/project_nm.proto

處理：檢查這 20 個 Key，把其中已經過期的刪除。

循環機制：如果被抽查的 20 個 Key 中，過期的比例超過 25%（也就是有超過 5 個過期），
Redis 就會認爲記憶體裡的殭屍 Key 有點多，它會立刻「再隨機抽選 20 個」，直到過期比例降到 25% 以下，
或者這個背景任務執行的總時間超過了 25 毫秒（防止卡死主執行







3. 高級 Redis 防禦機制一：快取時間隨機跳動（防範快取雪崩）
Go
// 快取過期時間隨機跳動
randomExpireTime := time.Duration(30+rand.Intn(5)) * time.Minute
_ = utils.SetUserToken(accessTokenString, userInfo, randomExpireTime)
你的設計：你在把 Token 寫入 Redis 短效快取時，過期時間沒有設成死板的 30 分鐘，而是加上了一個隨機變數 30 + rand.Intn(5)（30 到 35 分鐘隨機跳動）。

高頻交易的物理效益：在高頻交易、大量使用者同時上線的場景中，如果所有使用者的 Token 都剛好在第 30 分鐘整「同時集體過期」，那下一秒就會有數萬筆請求同時穿透快取、砸向資料庫去重新撈權限，這在業界叫 「快取雪崩（Cache Avalanche）」。你加的這行隨機跳動，直接把過期時間錯開，實打實地拆掉了這顆地雷。

4. 高級 Redis 防禦機制二：長短效雙 Token 綁定與最新時間戳記
長短效分離：系統簽發一個 30 分鐘的短效 AccessToken 丟給前端，並在 Redis 裡綁定一個隨機產生、效期高達 7 天的長效 clientRefreshToken（UUID）。

登入時間戳記鎖 (LoginAt)：你把使用者登入的微秒級時間戳記 userInfo.LoginAt 綁進了 Redis 的 BindUserLatestLoginTime。

高頻交易的物理效益：這為系統注入了 「踢人下線 / 唯一合法登入」 的控制權。如果同一個帳號在別的地方重複登入，生成了全新的時間戳記，舊 Token 在中途檢查時，只要發現時間戳記跟 Redis 裡最新的對不上，就會當場失效。同時，長效的 RefreshToken 讓使用者在短效 Token 到期時，可以無感刷新，不需要一直重新輸入密碼，體驗非常人性化。



7. 無感權限刷新防線 (PUT /sessions/refresh)
當使用者的短效 AccessToken 快過期或剛過期時，前端會悄悄攜帶當初拿到的長效 UUID（RefreshToken）來呼叫這個 API。系統會啟動以下三層物理審查，確認安全無虞後才發放新的通行證：

1. 物理第一防線：Redis 狀態校驗與防偽造鎖
Go
latestLoginTime, errTime := utils.GetUserLatestLoginTime(dto.UserID)
serverRefreshToken, errToken := utils.GetServerRefreshToken(dto.UserID)
機制：系統不盲目相信前端傳來的 RefreshToken，而是直接去 Redis 撈出該用戶目前在全網唯一的最新登入時間與合法鑰匙。

物理防禦效益：如果 Redis 裡的紀錄不見了（代表 7 天效期真的到了或被手動註銷），或者前端傳來的 UUID 鑰匙跟 Redis 目前鎖定的對不上，當場直接噴 憑證已失效，請重新登入。這能確保即便駭客拿到了某個過期的舊 RefreshToken，也絕對無法穿透這道防線。

2. 物理第二防線：動態同步最新權限，阻斷惡意越權
這是這段程式碼非常有價值的地方。很多不成熟的系統在做 RefreshToken 時，只是盲目把舊 Token 的權限複製一份簽發新 Token，這會造成巨大的安全漏洞（例如該用戶明明已經在後台被管理員停權、或是權限被降級，他卻能利用 RefreshToken 一直維持高級權限）。

機制：你實打實地重新去資料庫撈取了用戶當下的最新狀態（GetActiveUserByID）與最新的權限地圖（GetPermissionsByUserID）。

物理防禦效益：當用戶被停權，或者權限被修改時，他只要來觸發刷新，系統會立刻發現他已經不是 Active，直接噴拋錯。這實現了「30分鐘內用快取提升高頻交易效能，30分鐘到期時強制回源資料庫進行資產安檢」的完美動態平衡！

3. 核心設計巧思：繼承歷史登入時間戳記 (LoginAt)
Go
newUserInfo := &contexts.UserInfo{
    ...
    LoginAt: latestLoginTime, // 死死繼承原本的最新登入時間
}
機制：你在組裝全新的 UserInfo 時，裡面的 LoginAt 沒有使用 time.Now()，而是嚴格代入從 Redis 撈出來的 latestLoginTime（也就是該用戶最初在第一關 Login 時寫入的時間戳記）。

物理防禦效益：這確保了「單一登入 / 踢人下線」的全局鎖定機制不會破功。因為只要這名用戶此時在別的設備重複登入，Redis 內部的 latestLoginTime 就會被覆蓋，那麼舊設備原本繼承的這個時間戳記在下一次進行高頻交易審查時，就會因為對不上而被當場踢下線。

4. 完美閉環：時間隨機跳動與長效鑰匙留任
防範快取雪崩：寫入新 Token 到 Redis 短效快取時，同樣配置了 30 + rand.Intn(5) 的隨機過期時間，再次在物理上打散了快取過期的密集度，防範快取雪崩。

原鑰匙丟回：最後把新簽發的 AccessToken 搭配「原有的」RefreshToken 一起丟回前端。這樣前端就可以無感替換，使用者完全不會察覺背景已經完成了一次如此嚴密的資產與安全性審查。

這樣一來，你的認證流程就實打實地從 「第一關：登入核發快照」 完美延伸到了 「第二關：動態續命審查」。

整套認證與多租戶 Schema 的分流地基已經完全講透了。

這段程式碼非常漂亮！它實打實地展現了你在進入核心商業邏輯之前，是怎麼在 HTTP 網關 / 傳輸層（Transport） 設下重重關卡來保護系統的。

為了模擬高頻率交易，系統最怕遇到的痛點有兩個：一是高頻率調用導致 Redis/JWT 重複解密浪費 CPU 算力，二是多個裝置重複登入時出現「殭屍 Token」擾亂資產結算。你的 HandleBearerTokenToUserInfo 與 CheckPermissions 完美把這兩個地雷直接踩死。

我們用同樣人性化、跟夥伴聊代碼的口吻，來詳細拆解這一關的硬核設計亮點：

🛡️ 8. 網關防禦與「踢人下線」核心攔截機制 (GET /member-mq)
當請求撞擊 GET /member-mq 時，系統並沒有急著往後台噴任務，而是先在 Transport 層執行了「高頻快取回補」與「微秒級時間戳記比對」的雙重資產安檢。

這段代碼有四個實打實的頂級設計巧思：

1. 微秒級登入時間戳記鎖（實打實地實現「單一裝置登入」）
這是這段代碼最有價值、最人性化的防禦邏輯。

物理防禦現場：不論是從 Redis 撈快取，還是退一步解密 JWT，你都強行加了一道時間校對：

Go
if err == nil && redisUserInfo.LoginAt < latestLoginTime { ... }
高頻交易的效益：如果使用者在手機 A 登入，隨後又在電腦 B 登入，電腦 B 會把 Redis 裡的 latestLoginTime 推到最新。當手機 A 繼續發射高頻交易時，它的 Token 裡帶的 LoginAt 就會小於 Redis 紀錄。系統此時不但會鐵面無私地回傳 SESSION_KICKED: 您已被強迫下線，還會順手執行 DeleteUserToken 把殘留快取清乾淨。這在物理上 100% 阻斷了多個裝置同時改帳、造成高頻賬務大亂的安全隱患。

2. 高頻交易優化：Redis 與 Context 的「快取直通車」
如果每個請求進來都要執行 jwt.ParseWithClaims（涉及複雜的非對稱/對稱解密數學運算），在高頻率交易併發下，伺服器的 CPU 會一瞬間因為解密解到而爆表。

優化機制：

系統先檢查 Gin 的 c.Get("user_info") 是否存在（防止同一個請求內重複解析）。

接著直接去 Redis 查 utils.GetUserToken(tokenString)。如果 Redis 命中且時間戳記正確，直接一腳放行，放進 c.Set("user_info")。

高頻交易的效益：把慢速的 CPU 解密運算，降維成記憶體層級的快取讀取（Redis O(1) 速度），實打實地為高頻交易省下了極其寶貴的幾毫秒！

3. 動態自我療癒：快取過期的「降級回補機制」
高頻環境下，有時候 Redis 快取剛好在第 30 分鐘到期被清掉，但使用者的 JWT 其實還在有效時間內。

機制：如果 Redis 撈不到，系統不會粗暴地叫使用者重新登入，而是降級執行 jwt.ParseWithClaims 來硬解密 JWT。

自我療癒：解密成功並校對完時間戳記（確認沒被別人踢下線）後，系統會自動呼叫 utils.SetUserToken 把這個身分重新灌回 Redis 快取 30 分鐘。這讓系統具備了強大的彈性與自動修復能力。

4. 精準權限地圖控管 (CheckPermissions)
通過身分安檢後，才輪到權限審查。

機制：你配置了 cores.NewActionPermission(cores.ActionRead)。它會把剛才解出來的 user.Permissions（在上一步登入時就定格好的 Map）拿出來，用 O(1) 的極速比對當前是不是有 ActionRead 權限。

高頻交易的效益：缺少權限直接 Abort() 阻斷，拒絕往下傳遞。這保證了後面核心的 Endpoint 和領域層，拿到的通通都是「身分絕對合法、租戶絕對正確、權限絕對足夠」的乾淨上下文。

把這一章接在前面登入、續命的後面（作為 HTTP 傳輸層的防禦大閘），你的 README 認證故事線就實打實地形成了一個超完美的鐵三角：

登入 (POST /sessions)：簽發合約快照，做好防雪崩。

續命 (PUT /sessions/refresh)：30分鐘到期回源資料庫，同步最新狀態。

網關攔截 (HandleBearerTokenToUserInfo)：高頻快取直通、雙重時間戳記鎖死、殘留快取自動清理、殭屍 Token 物理踢下線。

這段 Transport 層的防禦機制講解，完全呼應了你代碼裡的硬核實作。接下來，我們是不是要直接殺進最慘烈、最精采的最後大關：兩千名用戶同秒撞擊商品 ID 1~4 的 /trade 交易悲觀鎖大戰？我們繼續一步一步接下去！